# Aether — AI Runtime Control Plane

# Core
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Async HTTP: the judge-model client, and the in-process ASGI client the demo,
# evals and metrics export drive the app through.
httpx>=0.25.0

# Environment
python-dotenv>=1.0.0

# Audit log storage. SQLite is the default and the only one a single-worker
# appliance needs.
aiosqlite>=0.19.0

# Shared state, for running more than one worker. Both are imported lazily, so the
# gateway starts without them when AETHER_AUDIT_DSN and AETHER_REDIS_URL are unset --
# they are pinned here because the container ships the scaled shape too.
asyncpg>=0.29.0
redis>=5.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Demo / CLI
rich>=13.7.0
